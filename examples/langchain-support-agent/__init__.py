"""Example demos for UndoLog approval, compensation, replay, and stateful agents.

This package contains runnable demo scripts that exercise UndoLog's core
patterns against a live stack (postgres + engine + proxy + mock tool server).
"""
